# DESIGN.md — OpenHarness 视觉世界

> 2026-09 重设计：从"AI 紫渐变"世界切换到"锌灰 + 深松绿"工坊风。旧紫配色仅作反参考。

## Design Read

桌面开发者工具（Agent Harness 集成）的产品界面，Operate 模式：可扫读性、精确、克制优先于表达。受众为技术用户。

## 色彩

- **中性底：锌灰（zinc）家族**，明暗两套。禁纯黑 #000 / 纯白 #fff，一律用 zinc-900/50 级别的近黑近白。
- **单一强调色：深松绿 `#2d6a4f`**（饱和度 <80%，白字对比 6.3:1 过 AA）。暗色模式用提亮档 `#38805c`（白字 4.8:1），保证按钮可读。
- **语义色锁定**：绿 `#16a34a` 仅用于安装/在线状态点（比强调色更亮，可区分）；红 `#dc2626` 仅用于危险操作。强调色不与语义色混用。
- 一个项目一套调色板；不做渐变、不做外发光、阴影以背景色调染色（rgba zinc）。
- 紫色系（#6c5ce7 家族）与烧陶土橙（#b5532b 家族）均为已废弃的反参考，不得回潮。

## 形状

- 圆角三档：8px（输入/小件）、10px（按钮/导航件）、14px（卡片/对话框）。全站统一，不混搭。

## 排版

- 界面字体：PingFang SC / Microsoft YaHei / system-ui；数字启用 tabular-nums。
- 路径、命令、配置值一律等宽（Consolas / JetBrains Mono）。
- 页面标题 24px/700/-0.02em；卡片标题 15-16px/600。强调靠字重与字号，不靠颜色渐变。

## 动效

- 单一入场时刻：页面挂载 page-in（0.32s ease-out，位移+透明度，无 fill-mode 残留）。
- 悬浮/按压反馈走 transform + 背景/边框色过渡；尊重 prefers-reduced-motion。

## 组件约定

- **无边框窗口**：`titleBarStyle: 'hidden'` + Windows 窗口控制按钮覆盖层；顶部 36px 为全局拖拽区（`.drag-strip`），覆盖层颜色经 IPC 随主题同步（`app:syncThemeOverlay`）。页面内容从 36px 下方开始排布。
- **单一平面**：侧栏、内容区、会话栏、工具栏共用同一背景色，无背景色分层、无结构性分隔线；层级关系只用 1px hairline 边框和留白表达，卡片无阴影、无悬浮抬升。
- 卡片：hairline 边框 + 14px 圆角，悬浮仅边框加深。
- 空态：居中的图标底座 + 标题 + 一句说明 + 就地主 CTA（`.empty-state`，main.scss）。
- 卡片操作区：右对齐按钮 + 1px 顶部分隔线。
- 品牌块（OH logo）：纯色绿底白字，无渐变无光晕。
