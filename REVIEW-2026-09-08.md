# OpenHarness 全项目检查、修复与复核

本次由单一代理执行，未使用子代理。采用 ponytail：先追踪真实调用链，只修可证实问题，复用已有实现和平台能力。

起始状态：`main`，`b7a64af`，工作区干净；基线测试 32/32。结束时 HEAD 未变，修改留在工作区，未暂存、提交或推送。

## 已修复问题

| 级别 | 原问题与触发场景 | 修复与证据 |
| --- | --- | --- |
| P1 | 旧 Electron / 构建链有 13 项依赖审计告警，含 1 critical、10 high、2 moderate；Electron 也属于实际运行时 | 升级 Electron 44.2.0、electron-builder 26.15.3、electron-vite 5.0.0、Vite 7.3.6；完整 `npm audit` 现为 0。告警数量不是 13 条独立可利用漏洞 |
| P1 | 原生窗口硬裁剪调用错误 DLL，实际调用直接抛 `Unable to find an entry point named 'CreateRectRgn' in DLL 'user32.dll'` | `win32-bridge.js` 改用 gdi32，设置区域失败时释放 GDI 对象；新增真正创建、销毁 Windows 区域的检查 |
| P1 | Hermes 的 `.json` 文件被 YAML 写入器改成 YAML，外部程序无法继续按 JSON 读取 | 复用 JSON 写入器；临时配置写入后重新 JSON.parse，验证原字段与新 token 保留 |
| P1 | 通用 db:set 能直接改 providers / proxyToken，绕过专用保存校验并造成运行中与磁盘配置不一致 | 通用接口仅允许读取 sessions/settings、写入 sessions 数组；测试拒绝特殊字段和嵌套键 |
| P2 | 先点 A 再点 B，A 检测较慢时会重新宣告自己为最新目标；退出工作台后也可能发生 | 最新目标在 IPC 入口确定；旧检测结果和旧原生窗口重激活均受同一条件约束 |
| P2 | 点击释放全部时，仍在启动的终端可能在清理之后创建，留下后台实例 | 先失效激活目标，等待在途打开，随后清理；释放期间拒绝新的打开；回归验证执行顺序 |
| P2 | Web Harness 启动超时只按尚未监听的端口清理，隐藏宿主仍被缓存，重试复用失败进程 | 超时时直接关闭对应 silent host；两次连续失败重试验证均重新启动并清理 |
| P2 | CLI 回退路径含空格时未加引号；旧命令缓存不更新；Pi/Kimi/MiniMax/Prime/DSH 首次安装没有配置就不能打开 | 共享解析器处理完整路径、去掉陈旧缓存，安装判定使用命令/候选可执行文件；临时带空格路径验证 |
| P2 | 对话提前断流、Responses failed/incomplete 被当作成功；Anthropic/Responses 完成事件后还等连接关闭 | 识别协议完成与失败事件；未收到完成标记的 EOF 报错；覆盖断流、失败以及完成后保持连接的情况 |
| P2 | 工作台卸载后迟到的加载/激活回调仍能重定位窗口；Web 标签显示无法实际完成的“独立窗口”操作 | 卸载使回调失效，只允许已就绪的原生标签转独立；终端销毁后清除实例引用 |
| P2 | 更新后的打包链重复源码编译 node-pty，因本机缺少 Spectre 库报 MSB8040 | 使用随包的原生预编译文件，关闭重复重编译；Electron 44 与最终 app.asar 中的 PTY 均真实运行通过 |

## Ponytail 清理

- `delete:` 删除未接入构建的 7za-shim 脚本、源码和二进制。旧补丁把解压失败改成成功，还硬编码本机路径。
- `shrink:` 模型诊断脚本直接使用生产 chat.js，删除另一套请求、降级与 SSE 解析实现。
- `shrink:` 删除 preload 第二次 JSON 往返；renderer/api.js 在跨 contextBridge 前已处理 Vue Proxy。真实页面发消息验证通过。
- `delete:` 删除无效 CLI 解析缓存、重复窗口 move、未使用的 Cherry 导入和多余 Store.initRenderer 调用。
- `shrink:` start 直接执行 electron-vite dev，去掉开发进程结束后再次启动旧产物的尾随命令。

本次 src、test-model.mjs 与 7za-shim 文本合计净减 74 行；未新增直接依赖。测试和锁文件变动单独计算，不把新增验证代码当成生产复杂度。

## 最终复核

| 检查 | 结果 |
| --- | --- |
| 主进程、代理、PTY、配置写入、全部适配器、preload、页面逻辑、辅助脚本与构建配置静态检查 | 完成；注册表中的 16 个适配器均纳入源码检查 |
| `npm test` | PASS，43/43，含真实 Windows GDI 调用 |
| `npm run build` | PASS |
| `npm run test:electron` | PASS，构建页面、preload IPC、聊天完成与保存调用、真实原生 PTY |
| `electron-builder --dir --publish never --config.directories.output=dist/review` | PASS，Windows x64 目录包 |
| `npm run test:electron -- dist/review/win-unpacked/resources/app.asar` | PASS，使用最终包内页面、preload、node-pty |
| `npm audit` | PASS，0 已知告警 |
| `git diff --check` | PASS |

Windows 目录包位于 `dist/review/win-unpacked`，不是已安装或已发布的版本。构建产物受现有 .gitignore 忽略。

## 边界与保留事项

- 未调用真实 Provider、未修改用户已有 Harness 配置；配置验证使用临时文件。Electron 聊天烟雾测试使用假上游与内存存储，并不证明真实模型质量或真实用户数据迁移。
- 未逐个启动外部 GUI Harness 做人工验收，也未覆盖多显示器/DPI/保存提示全过程。外部工具的各版本配置 schema 兼容性仍需实际集成验证；JSON 格式修复不等于已验证 Hermes 的所有配置字段。
- 跨 OpenAI/Anthropic 协议仍先取完整响应再输出 SSE，这是现有明确限制；本次没有增加另一套增量转换状态机。
- API Key 仍由 electron-store 本地明文保存，编辑页面可以读取。通用存储白名单不等于系统凭据加密，也不能抵御同账户恶意软件。
- node-pty 预编译方案本次验证范围为 Windows x64。新增原生依赖、更换架构或升级版本时，应先重新验证，必要时恢复源码重编译。
- 本次审计针对项目自有代码和依赖告警，未对全部第三方源码或图像/二进制素材逆向分析；0 依赖告警不代表所有运行场景均无风险。
